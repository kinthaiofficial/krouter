name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  frontend:
    name: Frontend build (dashboard)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Test
        working-directory: frontend
        run: npm test

      - name: Build
        working-directory: frontend
        run: npm run build

      - name: Upload dist
        uses: actions/upload-artifact@v4
        with:
          name: webui-dist
          path: internal/webui/dist/

  frontend-install:
    name: Frontend build (install wizard)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend-install/package-lock.json

      - name: Install dependencies
        working-directory: frontend-install
        run: npm ci

      - name: Test
        working-directory: frontend-install
        run: npm test

      - name: Build
        working-directory: frontend-install
        run: npm run build

      - name: Upload dist
        uses: actions/upload-artifact@v4
        with:
          name: installer-dist
          path: internal/webui/installer/dist/

  test:
    name: Test (${{ matrix.os }})
    needs: [frontend, frontend-install]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Download dashboard dist
        uses: actions/download-artifact@v4
        with:
          name: webui-dist
          path: internal/webui/dist/

      - name: Download installer dist
        uses: actions/download-artifact@v4
        with:
          name: installer-dist
          path: internal/webui/installer/dist/

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true

      - name: Build
        shell: bash
        run: go build ./cmd/krouter/... ./cmd/krouter-installer/... ./internal/... ./tests/...

      - name: Test
        shell: bash
        run: go test -race -coverprofile=coverage.out ./internal/...

      - name: Upload coverage
        if: matrix.os == 'ubuntu-latest'
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.out
        continue-on-error: true

  lint:
    name: Lint
    needs: [frontend, frontend-install]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download dashboard dist
        uses: actions/download-artifact@v4
        with:
          name: webui-dist
          path: internal/webui/dist/

      - name: Download installer dist
        uses: actions/download-artifact@v4
        with:
          name: installer-dist
          path: internal/webui/installer/dist/

      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --timeout=5m
